export enum STATUS {
    EMPTY = 'EMPTY',
    BLACK = 'BLACK',
    WHITE = 'WHITE',
}

export enum PLAYER {
    BLACK,
    WHITE,
}

export enum PlayStoneActionType {
    BLACK = 'BLACK',
    WHITE = 'WHITE',
}